export const blockCn = (name: string) => name;
export const elementCn = (parent: string, name: string) => `${parent}__${name}`;
export const modifierCn = (parent: string, name: string) =>
    `${parent}--${name}`;
